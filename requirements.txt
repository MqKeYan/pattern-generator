# ================================================================
# 斑图生成器 — Python 依赖清单
# ================================================================
# 安装:
#   pip install -r requirements.txt
#
# GPU 加速 (CUDA 12.x):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# 纯 CPU 模式:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

python_requires>=3.10

# ──────────────────────────────────────────────────────────────
# 计算引擎
# ──────────────────────────────────────────────────────────────
torch>=2.0.0,<3.0.0          # GPU/CPU 张量运算与自动微分
numpy>=1.24.0,<3.0.0          # 多维数组与数值计算

# ──────────────────────────────────────────────────────────────
# Web 框架
# ──────────────────────────────────────────────────────────────
fastapi>=0.110.0,<0.120.0     # 高性能异步 Web 框架
uvicorn[standard]>=0.27.0,<0.35.0  # ASGI 服务器 (含 websockets、httptools)
aiofiles>=23.0.0,<25.0.0      # 异步文件 I/O (StaticFiles 优化)

# ──────────────────────────────────────────────────────────────
# 系统监控
# ──────────────────────────────────────────────────────────────
psutil>=5.9.0,<7.0.0          # 跨平台系统资源监控 (CPU/内存)
