python_requires>=3.13

# 计算引擎
# GPU 加速 (CUDA 12.x): pip install torch --index-url https://download.pytorch.org/whl/cu121
# 纯 CPU 模式: pip install torch --index-url https://download.pytorch.org/whl/cpu
numpy>=1.24.0              # 多维数组与数值计算
torch>=2.0.0               # GPU/CPU张量计算

# Web 框架
flask>=3.0.0               # Web服务器框架
waitress>=3.0.0             # 生产级WSGI服务器 (Windows兼容)

# 系统监控
psutil>=5.9.0              # 跨平台系统资源监控 (CPU/内存)

# 打包构建 (开发依赖)
pyinstaller>=6.0            # 打包为单文件 .exe
